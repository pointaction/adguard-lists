# Member lists for the combined whitelist -> pasallow-completewhitelist.txt
# One member name per line. Each name MUST also be a source in
# whitelist-sources.txt, so its pasallow-<name>.txt file exists to merge.
# Lines starting with # are ignored.

badblock

# Enable more once you've added them to whitelist-sources.txt and synced:
# referral
# h12-whitelist
