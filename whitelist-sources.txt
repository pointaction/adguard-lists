# Whitelist (allowlist) sources for the "Sync allowlists" workflow.
# One source per line:   <name>  <url>
#   <name> becomes the output file  pasallow-<name>.txt
#   Use simple names (letters, numbers, dashes) — no spaces.
# Lines starting with # are ignored. Add or remove sources any time.
# Any input format (plain domains, @@|domain^, @@||domain^, hosts) is
# normalized to clean @@||domain^ allow rules.

referral https://raw.githubusercontent.com/hagezi/dns-blocklists/main/wildcard/whitelist-referral-onlydomains.txt
https://raw.githubusercontent.com/hl2guide/AdGuard-Home-Whitelist/main/whitelist.txt
